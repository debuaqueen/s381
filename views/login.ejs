<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Login - Student Manager</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
  <div class="container mt-5 col-md-4">
    <h1 class="text-center mb-4">Student Manager Login</h1>

    <% if (error) { %>
      <div class="alert alert-danger"><%= error %></div>
    <% } %>

    <div class="card border-primary mb-4">
      <div class="card-header bg-primary text-white">
        <strong>Default Account (Demo)</strong>
      </div>
      <div class="card-body">
        <p class="mb-1"><strong>Username:</strong> <code>admin</code></p>
        <p class="mb-0"><strong>Password:</strong> <code>admin123</code></p>
      </div>
    </div>

    <form action="/login" method="POST">
      <div class="mb-3">
        <label class="form-label">Username</label>
        <input type="text" name="username" class="form-control" value="admin" required>
      </div>
      <div class="mb-3">
        <label class="form-label">Password</label>
        <input type="password" name="password" class="form-control" value="admin123" required>
      </div>
      <button type="submit" class="btn btn-primary w-100 mb-2">Login</button>
    </form>

    <div class="text-center">
      <a href="/reset-password" class="btn btn-outline-warning btn-sm">
        Reset Password to Default
      </a>
    </div>

    <!-- Remove signup link completely -->
  </div>
</body>
</html>
