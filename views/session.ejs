<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Session & Cookie Demo</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
</head>
<body class="bg-light">
  <div class="container mt-5">
    <div class="row">
      <div class="col-md-8 mx-auto">
        <div class="card shadow">
          <div class="card-header bg-primary text-white">
            <h3><i class="bi bi-cookie"></i> Session & Cookie Demo</h3>
          </div>
          <div class="card-body">
            <% if (user) { %>
              <div class="alert alert-success">
                <strong>Login Successful!</strong> You are logged in as: <strong><%= user.username %></strong>
              </div>

              <h5>Current Session Information:</h5>
              <ul class="list-group mb-4">
                <li class="list-group-item"><strong>Username:</strong> <%= user.username %></li>
                <li class="list-group-item"><strong>Session ID (stored in cookie):</strong> <code><%= req.sessionID %></code></li>
                <li class="list-group-item"><strong>Cookie Name:</strong> <code>connect.sid</code></li>
                <li class="list-group-item"><strong>Cookie Secure:</strong> Yeses</li>
                <li class="list-group-item"><strong>Cookie HttpOnly:</strong> es (cannot be accessed by JavaScript)</li>
                <li class="list-group-item"><strong>Expires:</strong> 24 hours</li>
              </ul>

              <p class="text-muted">
                <i class="bi bi-info-circle"></i> 
                Your browser automatically sends the encrypted <code>connect.sid</code> cookie with every request — 
                this is how session persistence works!
              </p>

              <a href="/students" class="btn btn-primary">Go to Student Manager</a>
              <a href="/logout" class="btn btn-outline-danger float-end">Logout</a>

            <% } else { %>
              <div class="alert alert-warning">
                <strong>Not logged in</strong> — Please login first.
              </div>
              <a href="/login" class="btn btn-primary">Go to Login</a>
            <% } %>
          </div>
        </div>

        <div class="mt-4 text-center text-muted small">
          <p>This page proves that session data is stored securely in browser cookies using <code>express-session</code></p>
        </div>
      </div>
    </div>
  </div>
</body>
</html>